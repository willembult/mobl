server server

import mobl::server
import hi

@service GET /
function index(req : Request, res : Response) {
  res.send("Hello");
}

@service GET /:name
function index(req : Request, res : Response) {
  res.send("Hello" + req.params.name);
}